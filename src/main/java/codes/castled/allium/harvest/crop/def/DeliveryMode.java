package codes.castled.allium.harvest.crop.def;

/** Where generated harvest drops are delivered. */
public enum DeliveryMode {
    DROP_AT_CROP,
    PLAYER_INVENTORY,
    INVENTORY_THEN_DROP,
    COMMANDS_ONLY
}
